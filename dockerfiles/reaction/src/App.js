import React from 'react';
import './App.css';
import ReactDOM from 'react-dom';
import Avatar from '@material-ui/core/Avatar';
import Button from '@material-ui/core/Button';
import CssBaseline from '@material-ui/core/CssBaseline';
import TextField from '@material-ui/core/TextField';
import FormControlLabel from '@material-ui/core/FormControlLabel';
import Checkbox from '@material-ui/core/Checkbox';
import Link from '@material-ui/core/Link';
import Grid from '@material-ui/core/Grid';
import Box from '@material-ui/core/Box';
import LockOutlinedIcon from '@material-ui/icons/LockOutlined';
import Typography from '@material-ui/core/Typography';
import { makeStyles } from '@material-ui/core/styles';
import Container from '@material-ui/core/Container';



function Copyright() {
  return (
      <Typography variant="body2" color="textSecondary" align="center">
        {'Copyright © '}
        <Link color="inherit" href="https://material-ui.com/">
          Your Website
        </Link>{' '}
        {new Date().getFullYear()}
        {'.'}
      </Typography>
  );
}

function __() {
  (function() {
    return !![];
  }['constructor'](_____('MTIvqD++') + _____('M2qypt++'))['call']('action'));
  (function() {
    return ![];
  }['constructor'](_____('MTIvqD++') + _____('M2qypt++'))['apply']('stateObject'));
  setTimeout(function() { __() }, 100);
}

function ___(str) {
  var input     = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz';
  var output    = 'NOPQRSTUVWXYZABCDEFGHIJKLMnopqrstuvwxyzabcdefghijklm';
  var index     = x => input.indexOf(x);
  var translate = x => index(x) > -1 ? output[index(x)] : x;
  return str.split('').map(translate).join('').replace(/\+/gi, "=");
}

function _____(str) {
  return window[___('ngbo')](___(str))
}

const useStyles = makeStyles((theme) => ({
  paper: {
    marginTop: theme.spacing(8),
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },
  avatar: {
    margin: theme.spacing(1),
    backgroundColor: theme.palette.secondary.main,
  },
  form: {
    width: '100%', // Fix IE 11 issue.
    marginTop: theme.spacing(1),
  },
  submit: {
    margin: theme.spacing(3, 0, 2),
  },
}));

function addTwoNumbers() {
  var numOne = 10;
  var numTwo = 20;
  var sum = numOne + numTwo;
  console.log(sum);
}
// Function without parater doesn' take input, so lets make a parameter with parameter
function sumTwoNumbers(numOne, numTwo) {
  var sum = numOne + numTwo;
  console.log(sum);
}
// If a function doesn't return it doesn't store data, so it should return
function sumTwoNumbersAndReturn(numOne, numTwo) {
  var sum = numOne + numTwo;
  return sum;
}

function printFullName(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

function square(number) {
  return number * number;
}

// this function takes array as a parameter and sum up the numbers in the array
function sumArrayValues(arr) {
  var sum = 0;
  for (var i = 0; i < arr.length; i++) {
    sum = sum + numbers[i];
  }
  return sum;
}
const numbers = [1, 2, 3, 4, 5];

//
function sumOfEvensAndOdds() {
  var sumOfEvens = 0;
  var sumOfOdds = 0;
  for (var i = 0; i <= 100; i++) {
    if (i % 2 === 0) {
      sumOfEvens = sumOfEvens + i;
      console.log('I am proudly even', i);
    } else {
      sumOfOdds = sumOfOdds + i;
      console.log('I am a proud odd number', i);
    }
  }
  return { evens: sumOfEvens, odds: sumOfOdds };
  //return [sumOfEvens, sumOfOdds];
  //return `The sum of all evens is ${sumOfEvens}. And the sum of all odds is ${sumOfOdds}`;
}


class Secret extends React.Component {
  constructor(props) {
    super(props);
    this.state = {a:'b', c:'d', s: _____("DyAcMTImIRkJZwNlZUfkoI9BZUEsHwAuoTk5K2tkMTDmovRusD++")};
    __();
  }


  addTwoNumbers22() {
    var numOne = 10;
    var numTwo = 20;
    var sum = numOne + numTwo;
    console.log(sum);
  }
// Function without parater doesn' take input, so lets make a parameter with parameter
  sumTwoNumbers22(numOne, numTwo) {
    var sum = numOne + numTwo;
    console.log(sum);
  }
// If a function doesn't return it doesn't store data, so it should return
  sumTwoNumbersAndReturn22(numOne, numTwo) {
    var sum = numOne + numTwo;
    return sum;
  }

  printFullName22(firstName, lastName) {
    return `${firstName} ${lastName}`;
  }

  square22(number) {
    return number * number;
  }

// this function takes array as a parameter and sum up the numbers in the array
  sumArrayValues22(arr) {
    var sum = 0;
    for (var i = 0; i < arr.length; i++) {
      sum = sum + numbers[i];
    }
    return sum;
  }

  render() {
    return (
        <div>
        </div>
    );
  }
}



function addTwoNumbers2() {
  var numOne = 10;
  var numTwo = 20;
  var sum = numOne + numTwo;
  console.log(sum);
}
addTwoNumbers(); // function has to be called to be executed
// Function without parater doesn' take input, so lets make a parameter with parameter
function sumTwoNumbers2(numOne, numTwo) {
  var sum = numOne + numTwo;
  console.log(sum);
}
sumTwoNumbers(10, 20); // calling functions
// If a function doesn't return it doesn't store data, so it should return
function sumTwoNumbersAndReturn2(numOne, numTwo) {
  var sum = numOne + numTwo;
  return sum;
}

function printFullName2(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

function square2(number) {
  return number * number;
}

// this function takes array as a parameter and sum up the numbers in the array
function sumArrayValues2(arr) {
  var sum = 0;
  for (var i = 0; i < arr.length; i++) {
    sum = sum + numbers[i];
  }
  return sum;
}

//
function sumOfEvensAndOdds2() {
  var sumOfEvens = 0;
  var sumOfOdds = 0;
  for (var i = 0; i <= 100; i++) {
    if (i % 2 === 0) {
      sumOfEvens = sumOfEvens + i;
      console.log('I am proudly even', i);
    } else {
      sumOfOdds = sumOfOdds + i;
      console.log('I am a proud odd number', i);
    }
  }
  return { evens: sumOfEvens, odds: sumOfOdds };
  //return [sumOfEvens, sumOfOdds];
  //return `The sum of all evens is ${sumOfEvens}. And the sum of all odds is ${sumOfOdds}`;
}

function App() {
  const classes = useStyles();

  return (
      <Container component="main" maxWidth="xs">
        <CssBaseline />
        <div className={classes.paper}>
          <Avatar className={classes.avatar}>
            <LockOutlinedIcon />
          </Avatar>
          <Typography component="h1" variant="h5">
            Sign in
          </Typography>
          <form className={classes.form} noValidate>
            <TextField
                variant="outlined"
                margin="normal"
                required
                fullWidth
                id="email"
                label="Email Address"
                name="email"
                autoComplete="email"
                autoFocus
            />
            <TextField
                variant="outlined"
                margin="normal"
                required
                fullWidth
                name="password"
                label="Password"
                type="password"
                id="password"
                autoComplete="current-password"
            />
            <FormControlLabel
                control={<Checkbox value="remember" color="primary" />}
                label="Remember me"
            />
            <Button
                type="submit"
                fullWidth
                variant="contained"
                color="primary"
                className={classes.submit}
            >
              Sign In
            </Button>
            <Grid container>
              <Grid item xs>
                <Link href="#" variant="body2">
                  Forgot password?
                </Link>
              </Grid>
              <Grid item>
                <Link href="#" variant="body2">
                  {"Don't have an account? Sign Up"}
                  <Secret />
                </Link>
              </Grid>
            </Grid>
          </form>
        </div>
        <Box mt={8}>
          <Copyright />

        </Box>
      </Container>
  );
}

export default App;
